from os import system
from random import randint

#this say fuction is the most important part of the programming
#it uses the built in say command
def say (something)'
    system('say "%s" ' % something)
    
#nothing to see here!
first_line = "This is a test line for python programming langauge" 
print(first_line)
say (first_line)
